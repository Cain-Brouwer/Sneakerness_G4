// PostCSS configuration for Tailwind CSS.
// This enables the Tailwind PostCSS plugin so utility classes can be processed in the app.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
